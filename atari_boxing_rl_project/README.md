# Trained Models and Training Logs

The trained model files and full training logs are not included directly in this repository because they are generated artifacts and may be large.

They can be downloaded from the following link:

**Models and logs:**
https://finkiukim-my.sharepoint.com/:f:/g/personal/hristijan_jovanovski_2_students_finki_ukim_mk/IgDsTXq7bpb5S4tl11Ln74sTASq6_ccX-DiRhrnsLH9DceA?e=TlXil3


The models can also be regenerated by running:

```powershell
powershell -ExecutionPolicy Bypass -File .\run_professional_experiments.ps1 -IncludePPO -Include1M
```

The final CSV results and plots are included in the repository under the `results/` folder.
